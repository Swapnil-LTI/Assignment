import { Dashboard } from '@org/features';
import { SafeAreaView } from 'react-native-safe-area-context';


const DashboardScreen = () => {
    return (
        <SafeAreaView>
            <Dashboard/>
        </SafeAreaView>
    );
}

export default DashboardScreen;