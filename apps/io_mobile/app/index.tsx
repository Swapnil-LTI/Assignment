import { Landing } from '@org/features';
import { SafeAreaView } from 'react-native-safe-area-context';


const App = () => {
    return (
        <SafeAreaView>
            <Landing/>
        </SafeAreaView>
    );
}

export default App;